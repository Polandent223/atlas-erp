import fs from "node:fs";const a=fs.readFileSync("assets/cloud-auth.js","utf8"),s=fs.readFileSync("sql/ATLAS_SECURITY_AUTH_PHASE34.sql","utf8");for(const x of ["signInWithPassword","auth.getSession","profiles","auth.uid()","current_user_scope"])if(!(a+s).includes(x))throw new Error(x);if(a.toLowerCase().includes("service_role"))throw new Error("service_role");console.log("ATLAS Fase 34 auth contract OK");