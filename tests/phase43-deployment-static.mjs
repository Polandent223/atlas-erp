import fs from "node:fs";import path from "node:path";const html=fs.readFileSync("index.html","utf8");const refs=[...html.matchAll(/(?:src|href)=["']([^"'?#]+)["']/g)].map(x=>x[1]).filter(x=>!x.startsWith("http")&&!x.startsWith("data:")&&!x.startsWith("#"));const missing=refs.filter(r=>!fs.existsSync(path.resolve(r.replace(/^\.\//,""))));if(missing.length)throw new Error("Faltan: "+missing.join(","));for(const f of ["index.html","404.html","manifest.webmanifest","sw.js","assets/app.js","assets/data.js","assets/release-readiness.js"])if(!fs.existsSync(f))throw new Error("Falta "+f);const app=fs.readFileSync("assets/app.js","utf8");for(const x of ["dashboard","sales","purchases","inventory","cash","receivables","payables","accounting","reports","security","deploymentAudit"])if(!app.includes(x))throw new Error("Módulo "+x);console.log(`ATLAS Fase 43 static deployment OK (${refs.length} referencias)`);