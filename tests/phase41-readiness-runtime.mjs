globalThis.localStorage={data:new Map(),getItem(k){return this.data.has(k)?this.data.get(k):null},setItem(k,v){this.data.set(k,String(v))},removeItem(k){this.data.delete(k)}};const {DB}=await import("../assets/data.js");const {releaseReadiness}=await import("../assets/release-readiness.js");const s=DB.getState();if(s.schemaVersion!==41)throw new Error("schema");const r=releaseReadiness(s);if(!Array.isArray(r.gates)||r.gates.length!==5)throw new Error("gates");console.log(`ATLAS Fase 41 readiness runtime OK (${r.gates.filter(x=>x.ok).length}/5)`);