globalThis.localStorage={data:new Map(),getItem(k){return this.data.has(k)?this.data.get(k):null},setItem(k,v){this.data.set(k,String(v))},removeItem(k){this.data.delete(k)}};const {DB}=await import("../assets/data.js");const {runWorkflowAudit}=await import("../assets/workflow-audit.js");const s=DB.getState();if(s.schemaVersion!==38)throw new Error("schema");const a=runWorkflowAudit(s);if(!Array.isArray(a.checks)||a.checks.length<5)throw new Error("workflow audit");if(a.score<0||a.score>100)throw new Error("score");console.log(`ATLAS Fase 38 workflow audit OK (${a.score}%)`);